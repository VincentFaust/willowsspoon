version: 2

models:
  - name: dim_products
    columns:
      - name: product_key
        description: the surrogate key of the model using product_id 
        test:
          - not_null 
          - unique 

      - name: product_ id 
      - name: price 
      - name: grams 
      - name: handle 

