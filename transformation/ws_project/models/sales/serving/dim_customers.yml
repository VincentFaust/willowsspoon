version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_key
        description: the surrogate key of the model combining first name, last name and email 
        test:
          - not_null 
          - unique 
      - name: first_name 
      - name: last_name 
      - name: email 
      - name: total_spent 


