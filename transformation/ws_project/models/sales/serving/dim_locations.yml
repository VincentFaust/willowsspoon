version: 2

models:
  - name: dim_locations 
    columns:
      - name: location_key
        description: the surrogate key of the model using date_day 
        test:
          - not_null 
          - unique 

      - name: id
      - name: customer_id 
      - name: city 
      - name: province 
      - name: country_code 
      - name: zip 


